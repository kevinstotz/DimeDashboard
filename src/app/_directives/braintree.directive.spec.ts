import { BraintreeDirective } from './braintree.directive';

describe('BraintreeDirective', () => {
  it('should create an instance', () => {
    const directive = new BraintreeDirective();
    expect(directive).toBeTruthy();
  });
});
